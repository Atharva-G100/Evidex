{
  "name": "evidence-registry-backend",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "api": "node api/index.js",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "sync-artifacts": "node scripts/sync-artifacts.js",
    "deploy:all": "npm run deploy && npm run sync-artifacts"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "express": "^5.2.1",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.17.0"
  }
}
